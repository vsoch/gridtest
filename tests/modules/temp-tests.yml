temp:
  filename: /home/vanessa/Desktop/Code/gridtest/tests/modules/temp.py
  tests:
    temp.create_directory:
    - args:
        dirname: "{% tmp_dir %}"
    temp.write_file:
    - args:
        filename: "{% tmp_path %}"
